#!/usr/bin/python3


def multiple_returns(sentence):

    if sentence is None
        char = None

    if len(sentence) is 0:
        char = 0

    else:
        char = sentence[0]

    return (len(sentence), char) 
